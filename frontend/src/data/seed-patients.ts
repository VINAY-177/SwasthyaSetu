import { Patient, Screening, Referral } from "@/lib/types";

// Empty arrays for production deployment
export const seedPatients: Patient[] = [];
export const seedScreenings: Screening[] = [];
export const seedReferrals: Referral[] = [];
